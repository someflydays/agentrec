## What and why

<!-- What changes, and what problem it solves. Link the issue if there is one. -->

## How it was tested

<!-- Commands you ran, and what you verified by hand. For recorder or UI changes, say which
     terminal / browser you checked. -->

## Checklist

- [ ] `pnpm lint` passes
- [ ] `pnpm -r typecheck` passes
- [ ] `pnpm -r test` passes
- [ ] Tests added or updated for behavior changes
- [ ] Docs updated if behavior, commands, or the session format changed
- [ ] No change to the recorded session format — or `formatVersion` and `docs/format.md` were
      updated together
